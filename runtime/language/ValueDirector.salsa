module salsa_lite.runtime.language;

abstract behavior ValueDirector extends Director {
    abstract ack setValue(Object value);
    abstract ack addDirector(SynchronizationDirector director, int position);
}
